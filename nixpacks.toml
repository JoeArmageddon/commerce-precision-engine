[phases.build]
cmds = ["cd backend && pip install -r requirements.txt && prisma generate"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[start]
cmd = "cd backend && uvicorn src.main_standalone:app --host 0.0.0.0 --port ${PORT}"
